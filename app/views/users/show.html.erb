<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= @user.name %>
        (
        <% if @user.note_taker? %>
           <i class="cus-pencil"></i> NoteTaker
        <% elsif @user.admin?%>
           <i class="cus-cog-alt"></i> Admin
        <% else %>
           <i class="cus-user"></i> NoteUser
        <% end %>)
      </h1>
    </section>
    <% if current_user.admin? %>
      <section>
        <%= render partial: 'shared/course_feed_item', collection: @user.registered_courses.paginate(page: params[:page]) %>
      </section>
    <% end %>
  </aside>
  <div class="span8">
    <% if @user.notes.any? %>
      <h3>
        <%= pluralize(@user.notes.count, "Note") %> uploaded.
      </h3>
      <ol class="notes">
        <%= render @notes %>
      </ol>
      <%= will_paginate @notes %>
    <% end %>
  </div>
</div>